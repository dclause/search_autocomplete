# This file defines a default autocompletinfiguration for our module.

# This configuration autocompletes "out of the box" the drupal default
# search form.

# You can see where these properties are defined in the annotation of
# Drupal\search_autocomplete\Entity\AutocompletionConfiguration.

# The id of the config entity.
id: marvin
# Our properties follow.
label: 'Marvin, the paranoid android'
